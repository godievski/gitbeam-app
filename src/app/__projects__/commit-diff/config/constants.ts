import { CommitDiffItemState, CommitDiffState } from "./types";

export const DEFAULT_ITEM_STATE: CommitDiffItemState = {
  loading: false,
  entities: undefined,
};
export const DEFAULT_STATE: CommitDiffState = {};
