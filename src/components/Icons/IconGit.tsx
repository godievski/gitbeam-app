import { createIconSetFromFontello } from "react-native-vector-icons";

import giticonConfig from "../../fonts-config/giticon.config";

const IconGit = createIconSetFromFontello(giticonConfig);

export default IconGit;
